apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: properties-mongo-depl
spec:
  replicas: 1
  serviceName: properties-mongo-depl
  selector:
    matchLabels:
      app: properties-mongo
  template:
    metadata:
      labels:
        app: properties-mongo
    spec:
      containers:
        - name: properties-mongo
          image: mongo
          ports:
            - containerPort: 27017
          volumeMounts:
            - name: properties-mongo-data
              mountPath: /data/db
  volumeClaimTemplates:
    - metadata:
        name: properties-mongo-data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 500Mi
---
apiVersion: v1
kind: Service
metadata:
  name: properties-mongo-srv
spec:
  selector:
    app: properties-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017