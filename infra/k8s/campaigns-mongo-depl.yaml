apiVersion: apps/v1
kind: Deployment
metadata:
  name: campaigns-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: campaigns-mongo
  template:
    metadata:
      labels:
        app: campaigns-mongo
    spec:
      containers:
        - name: campaigns-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: campaigns-mongo-srv
spec:
  selector:
    app: campaigns-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017