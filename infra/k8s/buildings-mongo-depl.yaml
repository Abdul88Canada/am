apiVersion: apps/v1
kind: Deployment
metadata:
  name: buildings-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: buildings-mongo
  template:
    metadata:
      labels:
        app: buildings-mongo
    spec:
      containers:
        - name: buildings-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: buildings-mongo-srv
spec:
  selector:
    app: buildings-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017